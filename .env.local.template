# Copy this file to .env.local and fill in your values.
# Do not commit .env.local (it is in .gitignore).
# Same file is used by: agent.py and run_app.sh (which passes LiveKit/Google keys to the Flutter app).

# -----------------------------------------------------------------------------
# LiveKit (voice: app connects here; agent registers here)
# -----------------------------------------------------------------------------
LIVEKIT_URL=""
LIVEKIT_API_KEY=""
LIVEKIT_API_SECRET=""

# -----------------------------------------------------------------------------
# Google AI — Gemini LLM (agent)
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=""

# -----------------------------------------------------------------------------
# Google Maps — navigation (agent: start_navigation, get_walking_directions, search_places)
# Get key: https://console.cloud.google.com/ → project → APIs & Services → enable Directions API + Places API (New) → Credentials → Create API key
# -----------------------------------------------------------------------------
GOOGLE_MAPS_API_KEY=""

# -----------------------------------------------------------------------------
# Deepgram — speech-to-text (agent)
# https://console.deepgram.com/
# -----------------------------------------------------------------------------
DEEPGRAM_API_KEY=""

# -----------------------------------------------------------------------------
# ElevenLabs — text-to-speech (agent)
# https://elevenlabs.io/app/settings/api-keys
# Optional below: defaults in agent_config.py
# -----------------------------------------------------------------------------
ELEVEN_API_KEY=""
ELEVEN_VOICE_ID=""
ELEVEN_MODEL=""

# -----------------------------------------------------------------------------
# Backboard — memory (agent) — optional; omit to disable
# -----------------------------------------------------------------------------
BACKBOARD_API_KEY=""
