# Haptics

Import: `package:sesesesese/core/haptics/haptic_command_mapper.dart` → `handleHapticCommand`.  
Example: `handleHapticCommand("turn_left");`  
Commands: `turn_left`, `turn_right`, `danger`, `stop`, `cancel`.  
**Does not depend on camera or WebSocket code.**
